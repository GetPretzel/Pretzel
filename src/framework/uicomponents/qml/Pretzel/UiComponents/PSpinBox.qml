import QtQuick 2.15
import QtQuick.Controls 2.15
import QtQuick.Layouts 1.15

// NOTE: The following is temporary
// TODO: Create a custom spin box following Pretzel's UI/UX style guide
// TODO: Add the ability to open a popup when the "-" or "+" buttons are pressed
SpinBox {
    id: root

    height: 30
}
